# Routing is now category-based and profile-driven.
# Categories are hardcoded in inference/configs/user_classification_profiles.py
routing: {}

# Default chunking (can be overridden via .env.local)
chunking:
  strategy: "semantic"
  max_chunk_size: 4096
  overlap: 200
  threshold_tokens: 8000
